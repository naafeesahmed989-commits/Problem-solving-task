profit = 0
        min_price = prices[0]

        for price in prices:
            if price < min_price:
                min_price = price
            else:
                currProfit = price - min_price
                if currProfit > profit:
                    profit = currProfit

        return profit
